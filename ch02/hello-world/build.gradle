jar {
    manifest {
        attributes (
            'Main-Class' : 'by.kucher.springbook.ch02.helloworld.HelloWorld',
            'Class-Path' : configurations.compile.collect { it.getName() }.join(' ')
        )
    }
}